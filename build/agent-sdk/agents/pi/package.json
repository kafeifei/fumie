{
	"name": "agent-sdk-pi",
	"private": true,
	"comment": "Pinned dependency set for the build/agent-sdk Pi tarball. Fumie hosts the complete Pi coding-agent SDK as an Agent Host provider while keeping the renderer model catalog, AHP session catalog, worktree, and permission UI authoritative.",
	"dependencies": {
		"@earendil-works/pi-coding-agent": "0.84.2"
	}
}
